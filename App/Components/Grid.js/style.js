import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    row: {
        display: 'flex',
        flexDirection: 'row',
        flexWrap: 'wrap',
    },
    col: {
        position: 'relative',
        maxWidth: '100%',
    }
});
;